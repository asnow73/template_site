<header>
	<div class="navbar navbar-fixed-top navbar-inverse">
	  <div class="navbar-inner">
	    <div class="container">
	      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
	        <span class="icon-bar"></span>
	      </a>
	      <%= link_to "Домой", root_path, :class => "brand pull-left" %>
	      <div class="nav-collapse pull-right">
	        <div class="btn-group">
	        	<% if signed_in? %>
					    <button type="button" class="btn btn-default"><%= link_to "Выйти", auth_signout_path, method: "delete" %></button>
					  <% else %>
					    <button type="button" class="btn btn-default"><%= link_to "Войти", auth_signin_path %></button>
					  <% end %>>
					</div>
	      </div>
	    </div>
	  </div>
	</div>

	<ol class="breadcrumb">
		<%= render_breadcrumbs :tag => :li, :separator => " / " %>
	</ol>
</header>
